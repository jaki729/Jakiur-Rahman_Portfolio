## Core engineering theme

> Distributed systems, cloud-aware engineering.
>
> Real-Time Systems. Then make it fast, scalable and reliable.

## Run locally

Open `index.html` directly in a browser, or serve the folder with any static HTTP server.

For example:

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000`.

## Files

```text
index.html
static/
  app.js
  styles.css
README.md
.gitignore
```

## Content basis

Project descriptions are based on the supplied project documentation. Where the supplied material did not provide implementation details for a personal project, the portfolio keeps the description intentionally high-level rather than inventing technical claims.
